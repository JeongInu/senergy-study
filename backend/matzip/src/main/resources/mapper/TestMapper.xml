<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.matzip.mapper.TestMapper">

    <insert id="insertRestaurant" parameterType="Restaurant">
        INSERT INTO restaurants (restaurant_id, name, address, created_at, updated_at)
        VALUES (seq_restaurant_id.NEXTVAL, #{name}, #{address}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

</mapper>